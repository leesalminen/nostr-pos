name: nostr_pos_cli
description: Development CLI for the Nostr POS controller SDK.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  args: ^2.7.0
  nostr_pos:
    path: ../../packages/nostr_pos

dev_dependencies:
  lints: ^6.0.0
