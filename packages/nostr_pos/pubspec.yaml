name: nostr_pos
description: Controller SDK foundations for the open Nostr POS protocol.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  bip340: ^0.3.1
  crypto: ^3.0.7
  http: ^1.6.0
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
